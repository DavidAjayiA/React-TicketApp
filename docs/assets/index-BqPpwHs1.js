// Wrapper module to forward to the actual build bundle present in docs/assets
// This keeps the published site working even when the served index.html references
// a different hashed filename. It simply re-exports/imports the real bundle.
import './index-CAf6ZIyL.js';
